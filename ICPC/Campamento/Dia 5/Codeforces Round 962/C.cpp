#include <bits/stdc++.h>
using namespace std;

int main(){
    int t,n,q;
    cin>>t;
    while(t--){
        string s1,s2;
        cin>>n>>q;
        cin>>s1>>s2;
        while(q--){
            int l,r;
            cin>>l>>r;
            
        }

    }
}

// alguna manera de usar los sort que hice antes para calcular los nuevos
// cuantas veces se cumple que s1[i]!=s2[i] O(n)
