#include <bits/stdc++.h>
using namespace std;

int main(){
    // Representar un Grafo Explicitamente.
    int n,m; // Nodos y Aristas
    vector<vector<int>> graph (n);
    int u,v;
    for(int i=0;i<m;i++){
        cin>>u>>v;
        graph[u].push_back(v); // u esta conectado con v
        graph    
    }

    //Recorrer un Grafo

}